ADDITIONAL TASK - WE WANT TO DO AN IO.WRITER INJECTION TO CREATE A TEST OUTPUT FOR CONSOLE OUTPUT
logging package

Once we have the logs, we can build an automated test package - these tasks we may or may not want to do.

SUDO COMMAND TO ENTER OUR DATABASE with PSQL with PSPG as the pager:
sudo -u postgres PAGER=pspg PSQL_PAGER=pspg psql -d gator

For one off queries:
pgp is a bash script we wrote that enters pspg piped psql script

psql "postgres://postgres:{password}@localhost:5432/gator"  -  